/*
* ****************************************************
* * Aplicaci&oacute;n Base
* * Versi&oacute;n 1.0.0
* * Secretar&iacute;a de Gobernaci&oacute;n - DGTIC
* ****************************************************
*/
package mx.gob.segob.dgtic.comun.util.resteasy.token.constant;

/**
 * Clase que contiene la definici&oacute;n de constantes utilizadas para la construcci&oacute;n del token
 */
public final class TokenConstants {

	/**
	 * Restringe la construccion de la clase para evitar una creacion mediante el constructor por default.
	 */
	private TokenConstants(){
		throw new IllegalStateException("Constants class");
	}

	/**
	 * Constante que representa el atributo TOKEN_SECURITY_ENABLED_PROPERTY_NAME, dentro del archivo de propiedades de configuraci&oacute;n se identifica con el nombre "token.security.enabled". 
	 */
	public static final String TOKEN_SECURITY_ENABLED_PROPERTY_NAME = "token.security.enabled";

	/**
	 * Constante que representa el atributo TOKEN_KEY_SECRET_PROPERTY_NAME, dentro del archivo de propiedades de configuraci&oacute;n se identifica con el nombre "token.key.secret".
	 */
	public static final String TOKEN_KEY_SECRET_PROPERTY_NAME = "token.key.secret";

	/**
	 * Constante que representa el atributo TOKEN_AUTENTICACION_EXPIRA_PROPERTY_NAME, dentro del archivo de propiedades de configuraci&oacute;n se identifica con el nombre "token.autenticacion.expira.segundos".
	 */
	public static final String TOKEN_AUTENTICACION_EXPIRA_PROPERTY_NAME = "token.autenticacion.expira.segundos";

	/**
	 * Constante que representa el atributo TOKEN_ACCESO_EXPIRA_PROPERTY_NAME, dentro del archivo de propiedades de configuraci&oacute;n se identifica con el nombre "token.acceso.expira.segundos".
	 */
	public static final String TOKEN_ACCESO_EXPIRA_PROPERTY_NAME = "token.acceso.expira.segundos";


	/**
	 * Constante que representa el atributo JNDI_HOST_WS_PROPERTY_NAME, dentro del archivo de propiedades de configuraci&oacute;n se identifica con el nombre "token.enviroment.host".
	 */
	public static final String JNDI_HOST_WS_PROPERTY_NAME = "token.enviroment.host";

	/**
	 * Constante que representa el atributo AUTHORIZATION_HEADER_NAME, Nombre del Header que identifica el contenido del token de autorizaci&oacute;n en las peticiones restringidas". 
	 */
	public static final String AUTHORIZATION_HEADER_NAME = "Authorization";

	/**
	 * Constante que representa el atributo CLAIM_INFO_ADICIONAL_TOKEN_NAME, CLAIM que identifica el dato adicional dentro de la estructura del token".
	 */
	public static final String CLAIM_INFO_ADICIONAL_TOKEN_NAME = "info.adicional";

	/**
	 * Constante que representa el atributo AUTHENTICATION_SCHEME_NAME, Esquema que define el estandar por el cual se permite la autorizacion "Bearer Token"".
	 */
	public static final String AUTHENTICATION_SCHEME_NAME = "Bearer";


}
